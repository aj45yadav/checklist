import { SubQuestionsPipe } from './sub-questions.pipe';

describe('SubQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SubQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
