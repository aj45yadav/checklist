import { MainQuestionsPipe } from './main-questions.pipe';

describe('MainQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new MainQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
